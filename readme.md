# Servidor Web Linux Apache2

Restore the previously created snapshot in virtualbox;

Update the server;
Install apache2;
Copy the application files in the default apache directory;
Upload script file to a repository on GitHub.